services:
  watcher:
    build: .
    restart: unless-stopped
    volumes:
      - ./config.toml:/app/config.toml:ro
      - ./wallet.mnemonic:/app/wallet.mnemonic:ro
      - watcher-state:/app/data

volumes:
  watcher-state:
